
<!-- 英語ページ -->

<div id="one" class="none active">
  <table width="300" height="10", class="table table-default">
    <h2></h2>
    <thead>
      <th></th>
      <th>Name</th>
      <th>Tweet</th>
    </thead>
    
    <tbody>
      <% @search_posts.each do |post| %>
        <tr>
          <td>
            <%= attachment_image_tag post.user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg" ,:size => "40x40" %>
          </td>
          <td>
            <p class="result"><%= link_to post.user.name, user_path(post.user.id) %></p>
          </td>
          <td>
            <p class="result"><%= post.content %></p>
          </td>
          <td>
          </td>
        </tr>
      <% end %>
    </tbody>
    
  </table>
</div>


<!-- 日本語ページ -->
<div id="two" class="none">
  <table width="300" height="10", class="table table-default">
    <h2></h2>
    <thead>
      <th></th>
      <th>名前</th>
      <th>ツイート</th>
    </thead>
    
    <tbody>
      <% @search_posts.each do |post| %>
        <tr>
          <td>
            <%= attachment_image_tag post.user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg" ,:size => "40x40" %>
          </td>
          <td>
            <p class="result"><%= link_to post.user.name, user_path(post.user.id) %></p>
          </td>
          <td>
            <p class="result"><%= post.content %></p>
          </td>
          <td>
          </td>
        </tr>
      <% end %>
    </tbody>
    
  </table>
</div>